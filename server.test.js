const expect = require("chai").expect;

describe("Testing weather api", () => {
    it("should return weather", () => {
        expect(true).to.equal(true);
    });

    it("test network failure in weather api", () => {
        expect(true).to.equal(true);
    })
});
